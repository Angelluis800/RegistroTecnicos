@page "/Tecnico/Index"
@rendermode InteractiveServer
@inject TecnicoService tecnicoservices
@inject NavigationManager navigation

<PageTitle>T&eacute;cnicos Index</PageTitle>

<div class="container">
    <div class="card shadow-lg">
        @*Header*@
        <div class="card-header text-white text-center">
            <div class="bg-info rounded">                
                <h1>Registro de T&eacute;cnicos</h1>
            </div>           
        </div>
        @*Body*@
        <div class="card-body">
            <div class="text-end">
                <button class="btn btn-outline-success bi bi-person-fill-add" type="button" @onclick="Crear"> Crear</button>
            </div>
            <table class="table table-bordered">
                <thead class="bg-info text-center text-white">
                    <tr>
                        <th>Id</th>
                        <th>Nombre</th>
                        <th>Sueldo</th>
                        <th>Visualizar</th>
                        <th>Editar</th>
                        <th>Eliminar</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var t in ListaTecnicos)
                    {
                        <tr>
                            <td class="text-center">@t.TecnicoId</td>
                            <td class="text-center">@t.Nombres</td>
                            <td class="text-center">@t.SueldoHora.ToString("N2")</td>
                            <td class="text-center"><button type="button" class="btn btn-outline-dark bi bi-eye" @onclick="()=>Visualizar(t.TecnicoId)"></button></td>
                            <td class="text-center"><button type="button" class="btn btn-outline-warning bi bi-pencil-square" @onclick="()=>Editar(t.TecnicoId)"></button></td>
                            <td class="text-center"><button type="button" class="btn btn-outline-danger bi bi-trash" @onclick="()=>Eliminar(t.TecnicoId)"></button></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        @*Footer*@
        <div class="card-footer">            
            <p>Total de T&eacute;cnicos Ingresados: @ListaTecnicos.Count()</p>                                   
        </div>
    </div>
</div>

@code {
    public List<Tecnicos> ListaTecnicos = new List<Tecnicos>();

    protected override async Task OnInitializedAsync()
    {
        await Task.Delay(1000);
        ListaTecnicos = await tecnicoservices.Listar(e => e.TecnicoId > 0);
    }

    public void Crear()
    {
        navigation.NavigateTo("/Tecnico/Crear");
    }

    public void Visualizar(int id)
    {
        navigation.NavigateTo($"/Tecnico/Visualizar/tecnicoid={id}");
    }

    public void Editar(int id)
    {
        navigation.NavigateTo($"/Tecnico/Editar/tecnicoid={id}");
    }

    public void Eliminar(int id)
    {
        navigation.NavigateTo($"/Tecnico/Eliminar/tecnicoid={id}");
    }
}
